{
  "name": "jest-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
	"test": "jest",
	"coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "jest": {
    "collectCoverageFrom": [
      "**/*.js",
      "!data/keyMap.js",
      "!/node_modules/"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "save-dev": "0.0.1-security"
  },
  "devDependencies": {
    "coveralls": "^3.0.9",
    "jest": "^24.9.0"
  }
}
